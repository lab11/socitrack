Anchor BLE Application for TotTag
=================================

This app run on TotTag but puts it in anchor mode. On boot, the app sends the I2C commands
to put the SquarePoint module into anchor mode, and doesn't do much else.